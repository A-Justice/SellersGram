export function TopBadge() {
  return (
    <span className="inline-flex items-center rounded-full bg-gold px-2 py-0.5 text-[10px] font-semibold uppercase tracking-[0.14em] text-ink">
      Top
    </span>
  );
}
